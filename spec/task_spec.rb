# -*- coding: utf-8 -*-
require 'spec_helper'

# describe Task do
#   let(:task)   { Task.new params }
#   let(:params) { {tid: 'tasks', id: 123, name: '歯みがき', section: 1, elapsed: 0, done: false, estimate: 5} }

#   subject { task }

#   its(:tid)      { should == 'tasks' }
#   its(:rowid)    { should be_nil }
#   its(:id)       { should == 123 }
#   its(:name)     { should == '歯みがき' }
#   its(:section)  { should == 1 }
#   its(:elapsed)  { should == 0 }
#   its(:done)     { should == false }
#   its(:estimate) { should == 5 }
# end
